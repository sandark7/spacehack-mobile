import { Colors } from '../../Themes/'

export default {
  drawer: {
    backgroundColor: Colors.background,
    shadowColor: Colors.shadow,
    shadowRadius: 50,
    shadowOpacity: 1,
    shadowOffset: {
      width: 50
    }
  }
}
